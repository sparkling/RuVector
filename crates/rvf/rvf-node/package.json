{
  "name": "@ruvector/rvf-node",
  "version": "0.1.9-patch.9",
  "private": true,
  "description": "RuVector Format Node.js native bindings — BUILD-ONLY stub. Real redistribution at npm/packages/rvf-node/. Marked private so the ruflo-patch publish pipeline doesn't pick this location.",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "binaryName": "rvf-node",
    "targets": [
      "x86_64-unknown-linux-gnu",
      "x86_64-apple-darwin",
      "aarch64-apple-darwin",
      "x86_64-pc-windows-msvc",
      "aarch64-unknown-linux-gnu"
    ]
  },
  "scripts": {
    "build": "napi build --platform --release",
    "prepublishOnly": "napi prepublish -t npm"
  },
  "license": "MIT",
  "devDependencies": {
    "@napi-rs/cli": "^2.18.0"
  }
}
